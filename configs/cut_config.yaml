dataset:
  target_name: "cifar10_cut"
  target_folder: "data/cifar10_cut_32"

  # aux_name: "texture"
  # aux_folder: "data/texture_mnist_32"

  aux_name: "cifar10"
  aux_folder: "data/cifar10_v2"

  rng_seed: 72
  batch_size: 64
  base_sample_ratio: .1 # .2 means 20% of all train samples go to base

save_locations:
  model_folder: "models"
  file_folder: "files"
  image_folder: "images"
  logs_folder : "logs"

classifier:
  model: "resnet9"
  identifier: "Cifar10_Cut+Cifar10" # unique identifier for the set of models
  num_epochs: 100


unet:
  loss: "ebsw"
  num_epochs: 70

verbose: true
